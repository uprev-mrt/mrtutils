 # @file version.yml
 # @author generated by mrt-version (https://mrt.readthedocs.io/en/latest/pages/mrtutils/mrt-version.html)
 # @brief version header
 # @date ${t.getDate()}

---

major:  ${obj.major}
minor:  ${obj.minor}
patch:  ${obj.patch}
build:  ${obj.build}
branch: '${obj.branch}'
hash:   '${obj.hash}'
version_string: '${obj.major}.${obj.minor}.${obj.patch}.${obj.build}'

